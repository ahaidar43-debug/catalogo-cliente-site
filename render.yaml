services:
  - type: web
    name: catalogo-cliente
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /api/health
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: catalogo-cliente-db
          property: connectionString
      - key: SESSION_SECRET
        generateValue: true
      - key: ADMIN_USERNAME
        value: admin
      - key: ADMIN_PASSWORD
        sync: false
      - key: SELLER_USERNAME
        value: vendedora
      - key: SELLER_PASSWORD
        sync: false

databases:
  - name: catalogo-cliente-db
    plan: free
